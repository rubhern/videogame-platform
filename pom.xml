<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-parent</artifactId>
    <version>4.1.0</version>
    <relativePath/>
  </parent>

  <groupId>com.videogameplatform</groupId>
  <artifactId>videogame-platform</artifactId>
  <version>0.7.1-SNAPSHOT</version>
  <packaging>pom</packaging>

  <name>VideoGame Platform</name>
  <description>VideoGame Platform walking-skeleton build</description>

  <modules>
    <module>backend</module>
  </modules>

  <properties>
    <java.version>25</java.version>
    <maven.compiler.release>25</maven.compiler.release>
    <maven.compiler.enablePreview>false</maven.compiler.enablePreview>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <archunit.version>1.5.0</archunit.version>
    <spring-modulith.version>2.1.0</spring-modulith.version>
    <google-java-format.version>1.36.1</google-java-format.version>
    <jackson-bom.version>3.1.5</jackson-bom.version>
    <jacoco.version>0.8.15</jacoco.version>
    <log4j2.version>2.25.5</log4j2.version>
    <openapi-generator.version>7.24.0</openapi-generator.version>
    <postgresql.version>42.7.12</postgresql.version>
    <sonar-maven-plugin.version>5.7.0.6970</sonar-maven-plugin.version>
    <spotless-maven-plugin.version>3.9.0</spotless-maven-plugin.version>
    <source.revision>local-development</source.revision>
    <sonar.coverage.jacoco.xmlReportPaths>${maven.multiModuleProjectDirectory}/backend/target/site/jacoco/jacoco.xml</sonar.coverage.jacoco.xmlReportPaths>
    <sonar.exclusions>frontend/src/shared/api/generated/**,docs/architecture/api/reference/**,**/target/**,frontend/dist/**,frontend/playwright-report/**,frontend/test-results/**</sonar.exclusions>
    <sonar.host.url>https://sonarcloud.io</sonar.host.url>
    <sonar.maven.scanAll>true</sonar.maven.scanAll>
    <sonar.organization>rubhern</sonar.organization>
    <sonar.projectKey>rubhern_videogame-platform</sonar.projectKey>
    <sonar.projectName>VideoGame Platform</sonar.projectName>
    <sonar.scanner.skipJreProvisioning>true</sonar.scanner.skipJreProvisioning>
  </properties>

  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>org.springframework.modulith</groupId>
        <artifactId>spring-modulith-bom</artifactId>
        <version>${spring-modulith.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <build>
    <plugins>
      <plugin>
        <groupId>com.diffplug.spotless</groupId>
        <artifactId>spotless-maven-plugin</artifactId>
        <version>${spotless-maven-plugin.version}</version>
        <configuration>
          <java>
            <googleJavaFormat>
              <version>${google-java-format.version}</version>
              <style>AOSP</style>
              <formatJavadoc>false</formatJavadoc>
            </googleJavaFormat>
            <removeUnusedImports/>
            <forbidWildcardImports/>
          </java>
        </configuration>
        <executions>
          <execution>
            <id>verify-java-formatting</id>
            <goals>
              <goal>check</goal>
            </goals>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>org.jacoco</groupId>
        <artifactId>jacoco-maven-plugin</artifactId>
        <version>${jacoco.version}</version>
        <executions>
          <execution>
            <id>prepare-code-coverage</id>
            <goals>
              <goal>prepare-agent</goal>
            </goals>
          </execution>
          <execution>
            <id>report-code-coverage</id>
            <goals>
              <goal>report</goal>
            </goals>
            <configuration>
              <formats>
                <format>HTML</format>
                <format>XML</format>
              </formats>
            </configuration>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>org.sonarsource.scanner.maven</groupId>
        <artifactId>sonar-maven-plugin</artifactId>
        <version>${sonar-maven-plugin.version}</version>
      </plugin>
    </plugins>
  </build>
</project>

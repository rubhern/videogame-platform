package com.videogameplatform.catalogue.adapter.provider.igdb.model;

/** Keeps future IGDB transport models inside the provider adapter. */
final class IgdbProviderModelPlaceholder {

    private IgdbProviderModelPlaceholder() {}
}
